<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Client.Views">
    <Style TargetType="{x:Type local:InputBox}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:InputBox">
                    <Border CornerRadius="{TemplateBinding CornerRadius}"
                            Background="#3D3D3D"
                            Width="{TemplateBinding Width}" Height="{TemplateBinding Height}"
                            BorderBrush="Transparent" BorderThickness="1"
                            x:Name="Border">
                        <Grid Margin="2">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="6,0"
                                          VerticalAlignment="Center"/>

                            <TextBlock IsHitTestVisible="False" Margin="6,0"
                                       Text="{TemplateBinding Watermark}"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Foreground="WhiteSmoke"
                                       FontFamily="{StaticResource Besley}"
                                       FontSize="{TemplateBinding FontSize}"
                                       Opacity="0.6" Visibility="Collapsed"
                                       x:Name="TextBlock"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter TargetName="TextBlock" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="#569DE5"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#565656"/>
                            <Setter TargetName="TextBlock" Property="Foreground" Value="#565656"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="CaretBrush" Value="#569DE5"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="WhiteSmoke"/>
        <Setter Property="FontFamily" Value="{StaticResource Besley}"/>
    </Style>

    <Style TargetType="{x:Type local:RoundButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:RoundButton">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            Padding="{TemplateBinding Padding}"
                            x:Name="Border">
                        <TextBlock Text="{TemplateBinding Content}"
                                   Foreground="WhiteSmoke"
                                   HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                   FontFamily="{StaticResource Besley}" FontSize="{TemplateBinding FontSize}"
                                   x:Name="TextBlock" Margin="6,0"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#494949"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#444444"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="TextBlock" Property="Foreground" Value="#565656"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Padding" Value="2"/>
        <Setter Property="Background" Value="#3D3D3D"/>
    </Style>

    <Style TargetType="{x:Type local:PathButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:PathButton">
                    <Border Background="Transparent"
                            Height="{TemplateBinding Height}"
                            x:Name="Border">
                        <Path Data="{TemplateBinding Data}"
                              StrokeThickness="{TemplateBinding FontSize}"
                              Width="{Binding ElementName=Border, Path=ActualHeight}"
                              Stroke="{TemplateBinding Foreground}"
                              Stretch="Uniform"
                              VerticalAlignment="Center" HorizontalAlignment="Center"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              x:Name="Path"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Path" Property="Stroke" Value="#494949"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Path" Property="Stroke" Value="#444444"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Path" Property="Stroke" Value="#565656"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type Label}">
        <Setter Property="FontFamily" Value="{StaticResource Besley}"/>
        <Setter Property="Foreground" Value="WhiteSmoke"/>
    </Style>

    <Style TargetType="{x:Type Expander}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <Border x:Name="Border">
                        <StackPanel HorizontalAlignment="Stretch">
                            <ToggleButton IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                          HorizontalAlignment="Center"
                                          Padding="8,2">
                                <StackPanel Orientation="Horizontal" Margin="2">
                                    <ContentPresenter ContentSource="Header"
                                                      VerticalAlignment="Center"
                                                      TextElement.FontFamily="{StaticResource Besley}"
                                                      TextElement.FontWeight="{TemplateBinding FontWeight}"
                                                      TextElement.FontSize="{TemplateBinding FontSize}"
                                                      TextElement.Foreground="{TemplateBinding Foreground}"
                                                      Margin="0,0,3,0"/>

                                    <Path Width="{DynamicResource FontXSmall}"
                                          Height="{DynamicResource FontXSmall}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Stroke="{TemplateBinding Foreground}"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          StrokeThickness="{DynamicResource FontMicro}"
                                          Data="M 15 18 L 9 12 L 15 6"
                                          Stretch="Uniform"
                                          x:Name="Chevron"/>
                                </StackPanel>

                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="#1E1E1E"
                                                CornerRadius="5"
                                                VerticalAlignment="Center"
                                                Padding="{TemplateBinding Padding}"
                                                x:Name="Border">
                                            <ContentPresenter/>
                                        </Border>

                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#4F4F4F"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#444444"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter ContentSource="Content"
                                              Visibility="Collapsed"
                                              Margin="8,0"
                                              x:Name="ContentPresenter"/>
                        </StackPanel>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="True">
                            <Setter TargetName="ContentPresenter" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="Chevron" Property="Data" Value="M 6 9 L 12 15 L 18 9"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Foreground" Value="WhiteSmoke"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="FontSize" Value="11"/>
    </Style>

    <Style TargetType="{x:Type CheckBox}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal">
                        <Border Margin="0,0,5,0">
                            <Grid x:Name="Grid">
                                <Border BorderBrush="#444444"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Width="{Binding ElementName=Grid, Path=ActualHeight}"/>
                                <Border Background="Transparent"
                                        CornerRadius="3"
                                        x:Name="Border" Margin="2"/>
                            </Grid>
                        </Border>
                        
                        <ContentPresenter Grid.Column="1"
                                          Content="{TemplateBinding Content}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"
                                          TextElement.FontFamily="{StaticResource Besley}"
                                          TextElement.FontWeight="{TemplateBinding FontWeight}"
                                          TextElement.FontSize="{TemplateBinding FontSize}"
                                          TextElement.Foreground="WhiteSmoke"
                                          x:Name="ContentPresenter"/>
                    </StackPanel>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#444444"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#494949"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ContentPresenter" Property="TextElement.Foreground" Value="#565656"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>