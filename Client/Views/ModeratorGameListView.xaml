<UserControl x:Class="Client.Views.ModeratorGameListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1600">
    <Grid Margin="50,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" 
               Content="Games"
               HorizontalAlignment="Center"
               FontWeight="Black"
               FontSize="{StaticResource FontHuge}"/>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="GameList"></ItemsControl>
        </ScrollViewer>

        <StackPanel Orientation="Vertical" Grid.Row="2">
            <Separator Margin="{StaticResource MarginLargeVertical}"/>
            <Label Content="Create new game"
               HorizontalAlignment="Center"
               FontWeight="Bold"
               FontSize="{StaticResource FontLarge}"/>
            <Border Background="#282828" CornerRadius="{StaticResource RadiusLarge}"
                    Margin="{StaticResource MarginLargeVertical}"
                    Padding="{StaticResource MarginNormal}">
                <Grid Margin="{DynamicResource MarginNormal}">
                    <Label Content="57"
                           FontWeight="Black"
                           FontSize="{StaticResource FontXLarge}"
                           HorizontalAlignment="Left"/>
                    <local:RoundButton Content="Create"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center"
                                       CornerRadius="{StaticResource RadiusNormal}"
                                       FontSize="{StaticResource FontLarge}"
                                       FontWeight="Bold"
                                       Background="#3D6B3B"
                                       Margin="{StaticResource MarginLargeHorizontal}"
                                       x:Name="Create57Button"/>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
